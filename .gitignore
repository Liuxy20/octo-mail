# Build / test artifacts
/cover.html
*.test

# Local-only Go workspace (for mox co-development; not part of the build)
/go.work
/go.work.sum

# Runtime data directories (created by cmd/octo-mail defaults & tests)
/blobs/
/junk/
/acme/

# Editor / OS
.DS_Store
/octo-mail

# Generated shared-junk model packages are ignored except for the bundled
# default package embedded by source builds.
/junkfilter/models/*.csv.gz
!/junkfilter/models/shared-junk-v1.csv.gz

# Local secrets
/.env
