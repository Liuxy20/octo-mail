# Build / test artifacts
/cover.html
*.test

# Local-only Go workspace (for mox co-development; not part of the build)
/go.work
/go.work.sum

# Runtime data directories (created by cmd/octo-mail defaults & tests)
/blobs/
/junk/
/acme/

# Editor / OS
.DS_Store
/octo-mail

# Private release input: aggregate shared-junk model generated offline. The
# build embeds it when present, but the model is not published with source.
/junkfilter/models/*.csv.gz

# Local secrets
/.env
